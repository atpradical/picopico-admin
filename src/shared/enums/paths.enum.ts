export enum Paths {
  Home = '/',
  Payments = '/payments',
  Posts = '/posts',
  Users = '/users',
  logIn = '/auth/sign-in',
  statistics = '/statistics',
}

export enum BackButtonPathFlags {
  goBack = 'goBack',
  toProfile = 'toProfile',
  toSignUp = 'toSignUp',
}
