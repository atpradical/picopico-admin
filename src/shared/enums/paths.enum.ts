export enum Paths {
  Home = '/',
  Payments = '/payments',
  Posts = '/posts',
  Settings = '/settings',
  Users = '/users',
  logIn = '/auth/sign-in',
  profile = '/profile',
  statistics = '/statistics',
}

export enum BackButtonPathFlags {
  goBack = 'goBack',
  toProfile = 'toProfile',
  toSignUp = 'toSignUp',
}
